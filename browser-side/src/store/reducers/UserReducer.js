
const initialValue = {
    currentUser: null
}

const UserReducer = (state = initialValue, action) => {
    switch (action.type) {

        default:
            return { ...state }
    }
}

export default UserReducer;